import { FilterEmpPipe } from './filter-emp.pipe';

describe('FilterEmpPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmpPipe();
    expect(pipe).toBeTruthy();
  });
});
